/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'res.cloudinary.com', "assets.aceternity.com", "avatars.githubusercontent.com", "lh3.googleusercontent.com", "utfs.io"]
    }
};

export default nextConfig;
